<h1> PAID </h1>

<%= button_to "Add New Group", new_group_path, method: :get, class: 'info button' %>

<% @groups.each do |group| %>
  <%= button_to("#{group.name}", group_path(group), method: :get, class: 'info button') %>
<% end %>
